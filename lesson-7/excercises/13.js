/**
 * Write a function that receives three parameters: name, age and address
 * and returns an object with the values as the keys
 * 
 * example: buildPerson('Puki', 1, 'Santa Fe 940')
 * returns { name: 'Puki', age: 1, address: 'Santa Fe 940' }
 */