/**
  * a. Define a variable with an array
  * b. Investigate in MDN and use the following array functions in the array you defined:
  *   i. `.at()`
  *   ii. `.join()`
  *   iii. `.includes()`
  *
  */
