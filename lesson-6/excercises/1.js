/**
  * Define the following variables:
  *
  * - A variable str that is an string
  * - A variable number that is a number
  * - A variable float that is a decimal number
  * - A variable strArr that is an array with three strings inside
  * - A variable arr that contains six items of different types
  * - A variable bool that is a boolean
  * - A variable obj that is an object with 4 properties
  */

const str = "vero";

const number = 1;

const float = 1.5;

const dogs = ["puki", "kira", "tommy"];

const things = ["pencil", 1,5, "stars", 3, "puki", 1];

const bool = true;

const obj = {name:"tommy", age:"10", color:"grey", animal:"dog"}